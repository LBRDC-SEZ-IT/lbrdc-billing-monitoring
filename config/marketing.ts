import { MarketingConfig } from "@/types"

export const marketingConfig: MarketingConfig = {
  mainNav: [
    {
      title: "Outbound",
      href: "/outbound",
      visible: true,
    },
    {
      title: "Inbound",
      href: "/inbound",
      visible: true,
    }
  ]
}