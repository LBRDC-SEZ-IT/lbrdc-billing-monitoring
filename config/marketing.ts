import { MarketingConfig } from "@/types"

export const marketingConfig: MarketingConfig = {
  mainNav: [
    {
      title: "Outbound",
      href: "/outbound",
      visible: false,
    },
    {
      title: "Inbound",
      href: "/inbound",
      visible: false,
    }
  ]
}